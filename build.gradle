plugins {
    id 'net.researchgate.release' version '3.0.2'
    id 'uk.gov.laa.ccms.springboot.laa-ccms-spring-boot-gradle-plugin' version '0.0.25' apply false
}

subprojects {
    group = 'uk.gov.laa.ccms.soa.gateway'

    release {
        tagTemplate = '$name-$version'
    }
}
